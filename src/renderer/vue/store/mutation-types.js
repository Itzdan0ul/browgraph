'use strict';

/**
 * Settings
 */

const RENDER_SECTION = 'RENDER_SECTION';
const SET_FONT = 'SET_FONT';

/**
 * Pages: Home, Folder, Settings
 */

const RENDER_PAGE = 'RENDER_PAGE';

/**
 * Modal
 */

const RENDER_MODAL = 'RENDER_MODAL';

/**
 * Form
 */

const RENDER_FORM = 'RENDER_FORM';

/**
 * i18n
 */

const GET_MESSAGE = 'GET_MESSAGE';
const SELECT_LOCALE = 'SELECT_LOCALE';
const GET_CURRENT_LOCALE = 'GET_CURRENT_LOCALE';

/**
 * Folder
 */

const LOAD_PDF = 'LOAD_PDF';
const GET_PDF_PATH = 'GET_PDF_PATH';
const SAVE_PDF = 'SAVE_PDF';

export {
  RENDER_PAGE,
  SET_FONT,
  RENDER_SECTION,
  RENDER_MODAL,
  RENDER_FORM,
  GET_MESSAGE,
  SELECT_LOCALE,
  LOAD_PDF,
  GET_CURRENT_LOCALE,
  SAVE_PDF,
  GET_PDF_PATH,
};
